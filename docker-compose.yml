services:
  chatbot:
    build:
      context: .
    container_name: chatbot
    restart: unless-stopped
    ports:
      - "8001:8000"
    env_file:
      - .env
    environment:
      DB_PATH: /data/chatbot.db
    volumes:
      - ./chatbot-data:/data

volumes:
  chatbot-data:
